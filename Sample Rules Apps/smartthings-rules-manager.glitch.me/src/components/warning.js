import React from 'react';

export default class Nav extends React.Component {
  render() {
    return(
      <div class="alert alert-dark" role="alert">
        Note – this is an example OAuth integration. All personally-identifying information is stored locally, nothing is persisted on the server.
      </div>
    );
  }
}